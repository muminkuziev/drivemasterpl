# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Env files (secrets)
.env
.env.*
!.env.example

# Media (9GB+, uploaded separately to production disk, not committed)
apps/api/media/

# Mini App build copied into api/public-app during deploy (built artifact)
apps/api/public-app/

# Logs / OS junk
*.log
.DS_Store
Thumbs.db
